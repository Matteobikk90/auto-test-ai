{
  "name": "@repo/db",
  "version": "0.0.0",
  "scripts": {
    "db:r": "prisma migrate reset",
    "db:g": "prisma generate",
    "db:m": "prisma migrate dev --skip-generate",
    "db:d": "prisma migrate deploy"
  },
  "devDependencies": {
    "prisma": "^6.16.3"
  },
  "dependencies": {
    "@prisma/client": "^6.16.3"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
